const { addConfigFile } = require('./helpers');

// Test:
// no sizes.json
// with sizes.json
// multiple files

describe('add command', () => {
  describe('when there is no ')
  it('should', () => {
    console.log('running test');
    
    addConfigFile({files: ['test.txt']});
  })
})